<Window x:Class="cryptpad.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:cryptpad"
        mc:Ignorable="d"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Title="Password" Height="205" Width="300">
    <StackPanel Margin="5px">
        <PasswordBox Name="passwordBox" Padding="2px" />
        <Grid Margin="0,8,0,0">
            <Grid.Resources>
                <Style TargetType="{x:Type ComboBox}">
                    <Setter Property="Margin" Value="0,4,6,4" />
                </Style>
            </Grid.Resources>

            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <TextBlock Text="Algorithm" Grid.Column="0" Grid.Row="0" />
            <ComboBox Name="encAlgoComboBox" SelectionChanged="EncryptionAlgorithmSelected" Grid.Column="0" Grid.Row="1" />

            <TextBlock Text="Key size" Grid.Column="1" Grid.Row="0" />
            <ComboBox Name="keySizeComboBox" SelectionChanged="KeySizeSelected" Grid.Column="1" Grid.Row="1" />

            <TextBlock Text="Key algorithm" Grid.Column="0" Grid.Row="2" />
            <ComboBox Name="keyGenAlgoComboBox" SelectionChanged="KeyGenAlgorithmSelected" Grid.Column="0" Grid.Row="3" />

            <TextBlock Text="Text encoding" Grid.Column="1" Grid.Row="2" />
            <ComboBox Name="textEncodingComboBox" SelectionChanged="TextEncodingSelected" Grid.Column="1" Grid.Row="3">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding EncodingName}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button HorizontalAlignment="Right" Padding="2px" Margin="10,0,0,0" Click="CancelClicked">Cancel</Button>
            <Button IsDefault="True" HorizontalAlignment="Right" Padding="2px" Margin="10,0,0,0" Click="OKClicked">OK</Button>
        </StackPanel>
    </StackPanel>
</Window>
